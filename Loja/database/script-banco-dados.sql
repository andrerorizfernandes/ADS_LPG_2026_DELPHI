CREATE TABLE CARRO (
	CODIGO INTEGER GENERATED BY DEFAULT AS IDENTITY NOT NULL,
	NOME VARCHAR(50) NOT NULL,
	MARCA VARCHAR(30) NOT NULL,
	PLACA VARCHAR(7) NOT NULL,
	COR VARCHAR(20) NOT NULL,
	POTENCIA DECIMAL(10,2) NOT NULL,
	ANO INTEGER NOT NULL,
	CONSTRAINT PK_CARRO PRIMARY KEY (CODIGO)
);

CREATE TABLE MARCA (
	CODIGO INTEGER GENERATED BY DEFAULT AS IDENTITY NOT NULL,
	NOME VARCHAR(60) NOT NULL,
	CONSTRAINT MARCA_PK PRIMARY KEY (CODIGO),
	CONSTRAINT MARCA_UNIQUE UNIQUE (NOME)
);

ALTER TABLE CARRO ADD CODIGOMARCA INTEGER;

ALTER TABLE CARRO ADD CONSTRAINT FK_CARRO_MARCA
FOREIGN KEY (CODIGOMARCA) REFERENCES MARCA (CODIGO);

DELETE FROM CARRO;

ALTER TABLE CARRO ALTER CODIGOMARCA SET NOT NULL;

ALTER TABLE CARRO DROP MARCA;